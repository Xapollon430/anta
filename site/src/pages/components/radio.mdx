---
layout: ../../layouts/DocsLayout.astro
title: Radio
---
import Playground from '../../components/Playground'
import Preview from '../../components/Preview.astro'
import PropsTable from '../../components/PropsTable.astro'
import Disclosure from '../../components/Disclosure.astro'
import Columns from '../../components/Columns.astro'
import Col from '../../components/Col.astro'
import radioDemoCode from './radio.demo.ts'
import { RadioGroup, Radio, Icon } from '@antadesign/anta'

# Radio

A single option within a [`RadioGroup`](/components/radio-group/). The group owns selection, keyboard navigation, and the form value—a `Radio` is only interactive when placed in one.

<Disclosure title="Playground" anchor={false} brand>

<Playground
  client:visible
  component="Radio"
  layout="side"
  panelHeight={400}
  initialCode={radioDemoCode}
  initialCss={`html, body { height: 100%; }
.preview { min-height: 100%; align-items: center; justify-content: center; }`}
/>

</Disclosure>

## Label

<Columns>
<Col>

<Preview>
  <RadioGroup name="label-demo" defaultValue="text">
    <Radio value="text">Plain text label</Radio>
    <Radio value="rich">
      <span style={{ display: 'inline-flex', alignItems: 'center', gap: '4px' }}>
        With an icon <Icon shape="info" size={15} />
      </span>
    </Radio>
  </RadioGroup>
</Preview>

```tsx folded
<Radio value="text">Plain text label</Radio>
<Radio value="rich">
  <span style={{ display: 'inline-flex', alignItems: 'center', gap: '4px' }}>
    With an icon <Icon shape="info" size={15} />
  </span>
</Radio>
```

</Col>
<Col>

The label is `children`—text or any nodes. Use the slot for rich content like an icon or tooltip alongside the label.

</Col>
</Columns>

## Per-option tone & size

<Columns>
<Col>

<Preview>
  <RadioGroup name="override" defaultValue="a">
    <Radio value="a">Inherits the group</Radio>
    <Radio value="b" tone="neutral">Overrides to neutral</Radio>
    <Radio value="c" size="large">Overrides to large</Radio>
  </RadioGroup>
</Preview>

```tsx folded
<RadioGroup defaultValue="a">             {/* brand, medium */}
  <Radio value="a">Inherits the group</Radio>
  <Radio value="b" tone="neutral">Overrides to neutral</Radio>
  <Radio value="c" size="large">Overrides to large</Radio>
</RadioGroup>
```

</Col>
<Col>

Inherit the group's `tone` and `size`, or override on a single radio to call out a choice.

</Col>
</Columns>

## Disabled

<Columns>
<Col>

<Preview>
  <RadioGroup name="dis-one" defaultValue="a">
    <Radio value="a">Available</Radio>
    <Radio value="b" disabled>Unavailable</Radio>
  </RadioGroup>
</Preview>

```tsx folded
<Radio value="b" disabled>Unavailable</Radio>
```

</Col>
<Col>

Disable a single radio, or put `disabled` on the [`RadioGroup`](/components/radio-group/) to disable the whole set.

</Col>
</Columns>

## Targeting the selected state

<Columns>
<Col>

<Preview>
  <RadioGroup name="state-demo" defaultValue="b">
    <Radio value="a" className="selected-bold">First option</Radio>
    <Radio value="b" className="selected-bold">Second option</Radio>
    <Radio value="c" className="selected-bold">Third option</Radio>
  </RadioGroup>
  <style is:inline>{`
    a-radio.selected-bold:state(selected) { font-weight: 600; }
    a-radio.selected-bold[selected]      { font-weight: 600; }
  `}</style>
</Preview>

```css
a-radio:state(selected) { font-weight: 600; }
a-radio[selected]       { font-weight: 600; }  /* vanilla-HTML fallback */
```

</Col>
<Col>

Style the selected state with `:state(selected)` or `[selected]`. Use any CSS (weight, colors, animations, etc.).

</Col>
</Columns>

<Disclosure title="Component props" open>

<PropsTable component="Radio" />

</Disclosure>

<Disclosure title="Component tokens" open>

Override via inline style or CSS selector.

| Token | Description |
|---|---|
| `--radio-control-size` | Diameter of the circle (16 / 18 / 20px per `size`). |
| `--radio-dot-size` | Diameter of the selected centre dot. |
| `--radio-gap` | Space between the circle and the label. |
| `--radio-border-width` | Ring thickness. |
| `--radio-bg` | Unselected fill. |
| `--radio-border`<br />`--radio-border-hover`<br />`--radio-border-active` | Ring colour, per interaction state. |
| `--radio-bg-checked`<br />`--radio-bg-checked-hover`<br />`--radio-bg-checked-active` | Selected fill, per interaction state (re-pointed by `tone`). |
| `--radio-selected-border-hover`<br />`--radio-selected-border-active` | Ring colour while selected. |
| `--radio-dot` | Centre-dot colour. |
| `--radio-focus` | Focus-ring colour. |
| `--radio-bg-disabled`<br />`--radio-border-disabled`<br />`--radio-fg-disabled` | Disabled palette. |

</Disclosure>
